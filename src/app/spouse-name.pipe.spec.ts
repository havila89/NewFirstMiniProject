import { SpouseNamePipe } from './spouse-name.pipe';

describe('SpouseNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SpouseNamePipe();
    expect(pipe).toBeTruthy();
  });
});
